{
  "name": "ecommerce-root",
  "version": "1.0.0",
  "type": "module",
  "description": "Root workspace command to start  e-commerce store",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed": "node backend/seed-mongo.js",
    "seed:mongo": "node backend/seed-mongo.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.8.0",
    "multer": "^1.4.5-lts.1"
  }
}
